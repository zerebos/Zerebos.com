{
  "name": "zerebos.com",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "type": "module",
  "scripts": {
    "build": "bunx --bun astro build",
    "dev": "bunx --bun astro dev --port 6767",
    "preview": "bunx --bun astro preview --port 6767",
    "deploy": "bun run build && wrangler deploy",
    "check": "bunx --bun astro check",
    "check:links": "bun run build && bun run check:links:dist",
    "check:links:dist": "bun scripts/check-internal-links.ts",
    "check:assets": "bun scripts/find-unused-assets.ts",
    "wrangler:dev": "bun run build && wrangler dev",
    "wrangler:types": "wrangler types"
  },
  "author": "",
  "license": "",
  "devDependencies": {
    "@astrojs/check": "^0.9.8",
    "@astrojs/compiler-rs": "^0.1.10",
    "@zerebos/eslint-config": "^1.0.2",
    "@zerebos/eslint-config-typescript": "^1.0.3",
    "astro": "^6.1.9",
    "eslint": "^9.39.4",
    "wrangler": "^4.85.0"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.18",
    "@astrojs/sitemap": "^3.7.2"
  }
}